{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./", // Set the base directory
    "paths": {
      "assets/*": ["src/assets/*"] // Map 'assets' to 'src/assets'
    }
  }
}
